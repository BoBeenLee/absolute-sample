import React, { Component } from 'react';
import Layout from '../Layout';

class Main extends Component {
    render() {
        return (
            <Layout />
        );
    }
}

export default Main;
