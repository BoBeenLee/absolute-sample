import * as React from 'react';
import Header from '~/components/Header';

const Layout = () => {
    return (
        <div>
            <Header />
            Layout
        </div>
    );
};

export default Layout;
